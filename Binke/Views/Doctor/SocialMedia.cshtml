@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    ViewBag.Title = "Social Media";
}
@section pageheader{
    <div class="col-md-12" style="margin-bottom:25px;">
        <div class="col-md-3">
            <h3><i class="fa fa-user-md"></i>Social Media  </h3>
        </div>
        <div class="col-md-5 text-center" style="top:10px">
            <h3>@Convert.ToString(Session["DoctorName"])</h3>
        </div>
        <div class="col-md-4">
            <div class="breadcrumb-wrapper" style="top:15px;">
                <span class="label">You are here:</span>

                <ol class="breadcrumb">
                    <li> <a href='@("/DoctorProfile/"+Convert.ToString(Session["DoctorSearch"]))'> Doctor </a></li>
                    <li class="active">Social Media  </li>
                </ol>
            </div>
        </div>
    </div>
}


<div class="row" style="margin-top:20px;">
    <div class="col-lg-12">
        <div id="TopFormBlock" class="box-primary"></div>
        <br />
        <!--Tabs Content-->
        <div class="tab-content" style="display:none">
            <div id="DoctorSocialMedia" class="tab-pane fade active in">
                <table id="dtDoctorSocialMedia" class="table table-striped table-bordered table-hover" style="width:100%;">
                    <thead>
                        <tr>
                            <th>Facebook</th>
                            <th>Twitter</th>
                            @*<th>LinkedIn</th>*@
                            <th>Instagram</th>
                            <th>Youtube</th>
                            <th>Active</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>
        <!--===================================================-->
        <!--End Default Tabs (Left Aligned)-->
    </div>

</div>

@section pagespecific{
    <script type="text/javascript">
        //$("#MnuPharmacy").addClass('menu-open');
        //$("#MnuPharmacy ul li a p").each(function () {
        //    var text = $(this).text();
        //    if (text == "Social Media") {
        //        $(this).parent().addClass('active-sub-menu-item');
        //        $(this).parent().find('i').removeClass('far fa-circle');
        //        $(this).parent().find('i').addClass('far fa-dot-circle-o');
        //    }
        //    else {
        //        $(this).parent().removeClass('active-sub-menu-item');
        //        $(this).parent().find('i').removeClass('far fa-dot-circle-o');
        //        $(this).parent().find('i').addClass('far fa-circle');
        //    }
        //});

    $('#TopFormBlock').load('/DoctorSocialMedia/');

        $(document).ready(function () {
                        //bindDoctorSocialMediaList(true, 0);
                    });


        $(document).on('click', '.editInfo', function () {
                        var ItemId = $(this).attr('data-Id');
            $('#TopFormBlock').load('/DoctorSocialMedia/' + ItemId);
                    });

        $(document).on('click', '.viewInfo', function () {
                        var ItemId = $(this).attr('data-Id');
            $('#TopFormBlock').load('/DoctorSocialMedia/' + ItemId, function () {
                $("#TopFormBlock").find('input, textarea, button, select').attr("disabled", true);
                        });

                    });

        $(document).on('click', '.removeInfo', function () {
                        var ItemId = $(this).attr('data-Id');
                        var dataFlag = $(this).attr('data-flag');

                        if (dataFlag == "Activate")
                            DeactivateDoctorSocialMedia(ItemId, true);
                        else
                            DeactivateDoctorSocialMedia(ItemId, false);
                    });


                    //$("#AddPharmacy").click(function () {
                    //    window.loadRegisterForm("Pharmacy");
                    //});

                    //---
                    function bindDoctorSocialMediaList(flag, start) {
                        if ($.fn.DataTable.isDataTable("#dtDoctorSocialMedia")) {
                $('#dtDoctorSocialMedia').DataTable().destroy();
                    }
                    @*var table = $('#dtDoctorSocialMedia')
                        .DataTable({
                        "displayStart": start,
                    "sAjaxSource": "/GetDoctorSocialMediaList/" + flag + "/@ViewBag.DoctorID",
                    "columns": [
                        {
                            "data": "Facebook",
                            "autoWidth": false,
                            "searchable": true
                        },
                        {
                            "data": "Twitter",
                            "autoWidth": false,
                            "searchable": true
                        },
                        //{
                        //    "data": "LinkedIn",
                        //    "autoWidth": false,
                        //    "searchable": true
                        //},
                        {
                            "data": "Instagram",
                            "autoWidth": false,
                            "searchable": true
                        },
                        {
                            "data": "Youtube",
                            "autoWidth": false,
                            "searchable": true
                        },
                        {
                            "data": "IsActive",
                            "autoWidth": false,
                            "searchable": false,
                            "orderable": false,
                            "render": function (data, type, row) {
            var value = "";
            if (data) {
                value = "checked";
            }
            var content = '<label class="switch">&nbsp;';
            content += '<input type="checkbox" ' + value + ' data-Id= "IsActive" />&nbsp;';
            content += '<span class="slider round"style="width:35px;"></span> &nbsp;';
            content += ' ';
            content += '</label >';
            return content;
        }
                        },
                        {
                            "data": "SocialMediaId",
                            "autoWidth": false,
                            "searchable": false,
                            "orderable": false,
                            "render": function (data, type, row) {
            var content = '<div style="display:flex"><a href="JavaScript:" data-toggle="tooltip" data-original-title="Edit" data-flag="edit" data-Id="' + data + '" class="btn btn-primary btn-sm action editInfo"><i class="fa fa-edit"></i></a> &nbsp; ';
            content += '<a href="JavaScript:" data-toggle="tooltip" data-original-title="View" data-flag="View" data-Id="' + data + '" class="btn btn-success btn-sm action viewInfo"><i class="fa fa-eye"></i></a> &nbsp;';
            if (row.IsDeleted == false)
                content += '<a href="JavaScript:" data-toggle="tooltip" data-original-title="Delete" data-flag="DeActivate" data-Id="' + data + '" class="btn btn-danger btn-sm action removeInfo"><i class="fa fa-times"></i></a> &nbsp;</div>'
                                else
                content += '<a href="JavaScript:" data-toggle="tooltip" data-original-title="Re Activate" data-flag="Activate" data-Id="' + data + '" class="btn btn-danger btn-sm action removeInfo"><i class="fa fa-check"></i></a> &nbsp;</div>'

                                return content;
        }
                        }
                    ]
                });*@
        };

        function CurrentPageStart() {
            //var table = $('#dtDoctorSocialMedia').DataTable();
            //var info = table.page.info();
            //return info.start;
        }

        function DeactivateDoctorSocialMedia(id, flag) {
            var title = flag ? "ReActivate" : "Delete";
            var message = "Are you sure you want to <b> " + title + " </b> this Doctor Social Media Info";
            BinkeDialog.Confirmation(title, message, function () {
                $.ajax({
                url: "/ActiveDeActiveDoctorSocialMedia/" + flag + "/" + parseInt(id),
                    type: "POST",
                    contentType: false,
                    cache: false,
                    processData: false,
                    success: function (data) {
                        HandleResponse(data);
                        bindDoctorSocialMediaList(true, CurrentPageStart());
                        closeModal();
                        $('#TopFormBlock').load('/DoctorSocialMedia/');
                    },
                    error: function (data) {
                        HandleResponse(data);
                    }
                });
            });
        }
    </script>
}